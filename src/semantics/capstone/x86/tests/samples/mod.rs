use std::collections::BTreeMap;

use super::support::{
    I386Fixture, I386Register, assert_amd64_instruction_roundtrip_match_unicorn,
    assert_amd64_semantics_match_unicorn, assert_i386_instruction_roundtrip_match_unicorn,
    assert_i386_semantics_match_unicorn, assert_semantics_status,
};
use crate::Architecture;
use crate::semantics::SemanticStatus;

#[derive(Clone, Copy, Debug)]
pub(crate) struct X86FixtureSpec {
    pub registers: &'static [(I386Register, u128)],
    pub eflags: u32,
    pub memory: &'static [(u64, &'static [u8])],
}

impl From<X86FixtureSpec> for I386Fixture {
    fn from(spec: X86FixtureSpec) -> Self {
        Self {
            registers: spec.registers.to_vec(),
            eflags: spec.eflags,
            memory: spec
                .memory
                .iter()
                .map(|(address, bytes)| (*address, bytes.to_vec()))
                .collect(),
        }
    }
}

#[derive(Clone, Copy, Debug)]
pub(crate) struct X86Sample {
    pub mnemonic: &'static str,
    pub instruction: &'static str,
    pub architecture: Architecture,
    pub bytes: &'static [u8],
    pub expected_status: Option<SemanticStatus>,
    pub semantics_fixture: Option<X86FixtureSpec>,
    pub roundtrip_fixture: Option<X86FixtureSpec>,
}

pub(crate) fn assert_sample_statuses(samples: &[X86Sample]) {
    for sample in samples {
        if let Some(expected_status) = sample.expected_status {
            let sample_name = format!(
                "{} {}: {}",
                sample.architecture, sample.mnemonic, sample.instruction
            );
            assert_semantics_status(
                &sample_name,
                sample.architecture,
                sample.bytes,
                expected_status,
            );
        }
    }
}

pub(crate) fn assert_conformance_cases(samples: &[X86Sample]) {
    for sample in samples {
        if let Some(fixture) = sample.semantics_fixture {
            let sample_name = format!(
                "{} {}: {}",
                sample.architecture, sample.mnemonic, sample.instruction
            );
            match sample.architecture {
                Architecture::I386 => {
                    assert_i386_semantics_match_unicorn(&sample_name, sample.bytes, fixture.into())
                }
                Architecture::AMD64 => {
                    assert_amd64_semantics_match_unicorn(&sample_name, sample.bytes, fixture.into())
                }
                architecture => panic!("unsupported x86 sample architecture: {architecture}"),
            }
        }
    }
}

pub(crate) fn assert_roundtrip_cases(samples: &[X86Sample]) {
    for sample in samples {
        if let Some(fixture) = sample.roundtrip_fixture {
            let sample_name = format!(
                "{} {}: {}",
                sample.architecture, sample.mnemonic, sample.instruction
            );
            match sample.architecture {
                Architecture::I386 => assert_i386_instruction_roundtrip_match_unicorn(
                    &sample_name,
                    sample.bytes,
                    fixture.into(),
                ),
                Architecture::AMD64 => assert_amd64_instruction_roundtrip_match_unicorn(
                    &sample_name,
                    sample.bytes,
                    fixture.into(),
                ),
                architecture => panic!("unsupported x86 sample architecture: {architecture}"),
            }
        }
    }
}

#[allow(dead_code)]
pub(crate) fn sample_registry() -> BTreeMap<String, &'static [X86Sample]> {
    let mut samples = BTreeMap::new();
    samples.insert("aaa".to_string(), aaa::SAMPLES);
    samples.insert("aad".to_string(), aad::SAMPLES);
    samples.insert("aam".to_string(), aam::SAMPLES);
    samples.insert("aas".to_string(), aas::SAMPLES);
    samples.insert("adc".to_string(), adc::SAMPLES);
    samples.insert("adcx".to_string(), adcx::SAMPLES);
    samples.insert("add".to_string(), add::SAMPLES);
    samples.insert("addsubpd".to_string(), addsubpd::SAMPLES);
    samples.insert("addsubps".to_string(), addsubps::SAMPLES);
    samples.insert("addpd".to_string(), addpd::SAMPLES);
    samples.insert("addps".to_string(), addps::SAMPLES);
    samples.insert("addsd".to_string(), addsd::SAMPLES);
    samples.insert("addss".to_string(), addss::SAMPLES);
    samples.insert("adox".to_string(), adox::SAMPLES);
    samples.insert("and".to_string(), and::SAMPLES);
    samples.insert("andpd".to_string(), andpd::SAMPLES);
    samples.insert("andn".to_string(), andn::SAMPLES);
    samples.insert("andnpd".to_string(), andnpd::SAMPLES);
    samples.insert("andnps".to_string(), andnps::SAMPLES);
    samples.insert("andps".to_string(), andps::SAMPLES);
    samples.insert("blsi".to_string(), blsi::SAMPLES);
    samples.insert("blsmsk".to_string(), blsmsk::SAMPLES);
    samples.insert("blsr".to_string(), blsr::SAMPLES);
    samples.insert("bsf".to_string(), bsf::SAMPLES);
    samples.insert("bsr".to_string(), bsr::SAMPLES);
    samples.insert("bswap".to_string(), bswap::SAMPLES);
    samples.insert("bt".to_string(), bt::SAMPLES);
    samples.insert("call".to_string(), call::SAMPLES);
    samples.insert("cbw".to_string(), cbw::SAMPLES);
    samples.insert("cdq".to_string(), cdq::SAMPLES);
    samples.insert("cdqe".to_string(), cdqe::SAMPLES);
    samples.insert("btc".to_string(), btc::SAMPLES);
    samples.insert("btr".to_string(), btr::SAMPLES);
    samples.insert("bts".to_string(), bts::SAMPLES);
    samples.insert("bextr".to_string(), bextr::SAMPLES);
    samples.insert("bzhi".to_string(), bzhi::SAMPLES);
    samples.insert("cmovb".to_string(), cmovb::SAMPLES);
    samples.insert("cmovbe".to_string(), cmovbe::SAMPLES);
    samples.insert("cmovc".to_string(), cmovc::SAMPLES);
    samples.insert("cmove".to_string(), cmove::SAMPLES);
    samples.insert("cmovge".to_string(), cmovge::SAMPLES);
    samples.insert("cmovl".to_string(), cmovl::SAMPLES);
    samples.insert("cmovle".to_string(), cmovle::SAMPLES);
    samples.insert("cmovnb".to_string(), cmovnb::SAMPLES);
    samples.insert("cmovnbe".to_string(), cmovnbe::SAMPLES);
    samples.insert("cmovne".to_string(), cmovne::SAMPLES);
    samples.insert("cmovnl".to_string(), cmovnl::SAMPLES);
    samples.insert("cmovnle".to_string(), cmovnle::SAMPLES);
    samples.insert("cmovno".to_string(), cmovno::SAMPLES);
    samples.insert("cmovnp".to_string(), cmovnp::SAMPLES);
    samples.insert("cmovns".to_string(), cmovns::SAMPLES);
    samples.insert("cmovnz".to_string(), cmovnz::SAMPLES);
    samples.insert("cmovo".to_string(), cmovo::SAMPLES);
    samples.insert("cmovp".to_string(), cmovp::SAMPLES);
    samples.insert("cmovs".to_string(), cmovs::SAMPLES);
    samples.insert("cmovz".to_string(), cmovz::SAMPLES);
    samples.insert("cmp".to_string(), cmp::SAMPLES);
    samples.insert("cmps".to_string(), cmps::SAMPLES);
    samples.insert("cmpxchg".to_string(), cmpxchg::SAMPLES);
    samples.insert("cmpxchg16b".to_string(), cmpxchg16b::SAMPLES);
    samples.insert("cmpxchg8b".to_string(), cmpxchg8b::SAMPLES);
    samples.insert("cmpss".to_string(), cmpss::SAMPLES);
    samples.insert("comiss".to_string(), comiss::SAMPLES);
    samples.insert("comisd".to_string(), comisd::SAMPLES);
    samples.insert("cpuid".to_string(), cpuid::SAMPLES);
    samples.insert("crc32".to_string(), crc32::SAMPLES);
    samples.insert("cqo".to_string(), cqo::SAMPLES);
    samples.insert("cwd".to_string(), cwd::SAMPLES);
    samples.insert("cwde".to_string(), cwde::SAMPLES);
    samples.insert("cvtdq2pd".to_string(), cvtdq2pd::SAMPLES);
    samples.insert("cvtdq2ps".to_string(), cvtdq2ps::SAMPLES);
    samples.insert("cvtpd2dq".to_string(), cvtpd2dq::SAMPLES);
    samples.insert("cvtpd2ps".to_string(), cvtpd2ps::SAMPLES);
    samples.insert("cvtpi2pd".to_string(), cvtpi2pd::SAMPLES);
    samples.insert("cvtpi2ps".to_string(), cvtpi2ps::SAMPLES);
    samples.insert("cvtps2dq".to_string(), cvtps2dq::SAMPLES);
    samples.insert("cvtps2pd".to_string(), cvtps2pd::SAMPLES);
    samples.insert("cvtsd2si".to_string(), cvtsd2si::SAMPLES);
    samples.insert("cvtsd2ss".to_string(), cvtsd2ss::SAMPLES);
    samples.insert("cvtsi2sd".to_string(), cvtsi2sd::SAMPLES);
    samples.insert("cvtsi2ss".to_string(), cvtsi2ss::SAMPLES);
    samples.insert("cvtss2sd".to_string(), cvtss2sd::SAMPLES);
    samples.insert("cvtss2si".to_string(), cvtss2si::SAMPLES);
    samples.insert("cvttpd2dq".to_string(), cvttpd2dq::SAMPLES);
    samples.insert("cvttps2dq".to_string(), cvttps2dq::SAMPLES);
    samples.insert("cvttsd2si".to_string(), cvttsd2si::SAMPLES);
    samples.insert("cvttss2si".to_string(), cvttss2si::SAMPLES);
    samples.insert("daa".to_string(), daa::SAMPLES);
    samples.insert("dec".to_string(), dec::SAMPLES);
    samples.insert("div".to_string(), div::SAMPLES);
    samples.insert("divpd".to_string(), divpd::SAMPLES);
    samples.insert("divps".to_string(), divps::SAMPLES);
    samples.insert("divsd".to_string(), divsd::SAMPLES);
    samples.insert("divss".to_string(), divss::SAMPLES);
    samples.insert("enter".to_string(), enter::SAMPLES);
    samples.insert("extractps".to_string(), extractps::SAMPLES);
    samples.insert("fabs".to_string(), fabs::SAMPLES);
    samples.insert("f2xm1".to_string(), f2xm1::SAMPLES);
    samples.insert("fadd".to_string(), fadd::SAMPLES);
    samples.insert("faddp".to_string(), faddp::SAMPLES);
    samples.insert("fbld".to_string(), fbld::SAMPLES);
    samples.insert("fbstp".to_string(), fbstp::SAMPLES);
    samples.insert("fchs".to_string(), fchs::SAMPLES);
    samples.insert("fcom".to_string(), fcom::SAMPLES);
    samples.insert("fcomi".to_string(), fcomi::SAMPLES);
    samples.insert("fcomip".to_string(), fcomip::SAMPLES);
    samples.insert("fcmov".to_string(), fcmov::SAMPLES);
    samples.insert("fcmovb".to_string(), fcmovb::SAMPLES);
    samples.insert("fcmovbe".to_string(), fcmovbe::SAMPLES);
    samples.insert("fcmove".to_string(), fcmove::SAMPLES);
    samples.insert("fcmovnb".to_string(), fcmovnb::SAMPLES);
    samples.insert("fcmovnbe".to_string(), fcmovnbe::SAMPLES);
    samples.insert("fcmovne".to_string(), fcmovne::SAMPLES);
    samples.insert("fcmovnu".to_string(), fcmovnu::SAMPLES);
    samples.insert("fcmovu".to_string(), fcmovu::SAMPLES);
    samples.insert("fcomp".to_string(), fcomp::SAMPLES);
    samples.insert("fcompp".to_string(), fcompp::SAMPLES);
    samples.insert("fcos".to_string(), fcos::SAMPLES);
    samples.insert("fdecstp".to_string(), fdecstp::SAMPLES);
    samples.insert("fdiv".to_string(), fdiv::SAMPLES);
    samples.insert("fdivp".to_string(), fdivp::SAMPLES);
    samples.insert("fdivr".to_string(), fdivr::SAMPLES);
    samples.insert("fdivrp".to_string(), fdivrp::SAMPLES);
    samples.insert("ffree".to_string(), ffree::SAMPLES);
    samples.insert("ffreep".to_string(), ffreep::SAMPLES);
    samples.insert("fiadd".to_string(), fiadd::SAMPLES);
    samples.insert("fidiv".to_string(), fidiv::SAMPLES);
    samples.insert("fidivr".to_string(), fidivr::SAMPLES);
    samples.insert("fist".to_string(), fist::SAMPLES);
    samples.insert("fistp".to_string(), fistp::SAMPLES);
    samples.insert("fisttp".to_string(), fisttp::SAMPLES);
    samples.insert("fimul".to_string(), fimul::SAMPLES);
    samples.insert("fisub".to_string(), fisub::SAMPLES);
    samples.insert("fisubr".to_string(), fisubr::SAMPLES);
    samples.insert("fild".to_string(), fild::SAMPLES);
    samples.insert("fincstp".to_string(), fincstp::SAMPLES);
    samples.insert("fnclex".to_string(), fnclex::SAMPLES);
    samples.insert("fnop".to_string(), fnop::SAMPLES);
    samples.insert("fld".to_string(), fld::SAMPLES);
    samples.insert("fld1".to_string(), fld1::SAMPLES);
    samples.insert("fldl2e".to_string(), fldl2e::SAMPLES);
    samples.insert("fldl2t".to_string(), fldl2t::SAMPLES);
    samples.insert("fldlg2".to_string(), fldlg2::SAMPLES);
    samples.insert("fldln2".to_string(), fldln2::SAMPLES);
    samples.insert("fldpi".to_string(), fldpi::SAMPLES);
    samples.insert("fldz".to_string(), fldz::SAMPLES);
    samples.insert("fmul".to_string(), fmul::SAMPLES);
    samples.insert("fmulp".to_string(), fmulp::SAMPLES);
    samples.insert("fninit".to_string(), fninit::SAMPLES);
    samples.insert("fnstsw".to_string(), fnstsw::SAMPLES);
    samples.insert("fpatan".to_string(), fpatan::SAMPLES);
    samples.insert("fprem".to_string(), fprem::SAMPLES);
    samples.insert("fprem1".to_string(), fprem1::SAMPLES);
    samples.insert("fptan".to_string(), fptan::SAMPLES);
    samples.insert("frndint".to_string(), frndint::SAMPLES);
    samples.insert("fscale".to_string(), fscale::SAMPLES);
    samples.insert("fsin".to_string(), fsin::SAMPLES);
    samples.insert("fsincos".to_string(), fsincos::SAMPLES);
    samples.insert("fsqrt".to_string(), fsqrt::SAMPLES);
    samples.insert("fst".to_string(), fst::SAMPLES);
    samples.insert("fstp".to_string(), fstp::SAMPLES);
    samples.insert("ftst".to_string(), ftst::SAMPLES);
    samples.insert("fsub".to_string(), fsub::SAMPLES);
    samples.insert("fsubp".to_string(), fsubp::SAMPLES);
    samples.insert("fsubr".to_string(), fsubr::SAMPLES);
    samples.insert("fsubrp".to_string(), fsubrp::SAMPLES);
    samples.insert("fucom".to_string(), fucom::SAMPLES);
    samples.insert("fucomi".to_string(), fucomi::SAMPLES);
    samples.insert("fucomip".to_string(), fucomip::SAMPLES);
    samples.insert("fucomp".to_string(), fucomp::SAMPLES);
    samples.insert("fucompp".to_string(), fucompp::SAMPLES);
    samples.insert("fxam".to_string(), fxam::SAMPLES);
    samples.insert("fxch".to_string(), fxch::SAMPLES);
    samples.insert("fyl2x".to_string(), fyl2x::SAMPLES);
    samples.insert("fyl2xp1".to_string(), fyl2xp1::SAMPLES);
    samples.insert("haddpd".to_string(), haddpd::SAMPLES);
    samples.insert("haddps".to_string(), haddps::SAMPLES);
    samples.insert("idiv".to_string(), idiv::SAMPLES);
    samples.insert("inc".to_string(), inc::SAMPLES);
    samples.insert("int3".to_string(), int3::SAMPLES);
    samples.insert("jae".to_string(), jae::SAMPLES);
    samples.insert("je".to_string(), je::SAMPLES);
    samples.insert("jmp".to_string(), jmp::SAMPLES);
    samples.insert("jne".to_string(), jne::SAMPLES);
    samples.insert("kmovw".to_string(), kmovw::SAMPLES);
    samples.insert("lahf".to_string(), lahf::SAMPLES);
    samples.insert("lar".to_string(), lar::SAMPLES);
    samples.insert("lddqu".to_string(), lddqu::SAMPLES);
    samples.insert("ldmxcsr".to_string(), ldmxcsr::SAMPLES);
    samples.insert("leave".to_string(), leave::SAMPLES);
    samples.insert("lods".to_string(), lods::SAMPLES);
    samples.insert("lzcnt".to_string(), lzcnt::SAMPLES);
    samples.insert("minsd".to_string(), minsd::SAMPLES);
    samples.insert("minps".to_string(), minps::SAMPLES);
    samples.insert("minss".to_string(), minss::SAMPLES);
    samples.insert("maskmovq".to_string(), maskmovq::SAMPLES);
    samples.insert("movd".to_string(), movd::SAMPLES);
    samples.insert("movabs".to_string(), movabs::SAMPLES);
    samples.insert("movaps".to_string(), movaps::SAMPLES);
    samples.insert("movdq2q".to_string(), movdq2q::SAMPLES);
    samples.insert("movbe".to_string(), movbe::SAMPLES);
    samples.insert("movapd".to_string(), movapd::SAMPLES);
    samples.insert("movddup".to_string(), movddup::SAMPLES);
    samples.insert("movdqa".to_string(), movdqa::SAMPLES);
    samples.insert("movdqu".to_string(), movdqu::SAMPLES);
    samples.insert("movhlps".to_string(), movhlps::SAMPLES);
    samples.insert("movhpd".to_string(), movhpd::SAMPLES);
    samples.insert("movhps".to_string(), movhps::SAMPLES);
    samples.insert("movlhps".to_string(), movlhps::SAMPLES);
    samples.insert("movlpd".to_string(), movlpd::SAMPLES);
    samples.insert("movlps".to_string(), movlps::SAMPLES);
    samples.insert("movmskpd".to_string(), movmskpd::SAMPLES);
    samples.insert("movmskps".to_string(), movmskps::SAMPLES);
    samples.insert("movntdq".to_string(), movntdq::SAMPLES);
    samples.insert("movnti".to_string(), movnti::SAMPLES);
    samples.insert("movntpd".to_string(), movntpd::SAMPLES);
    samples.insert("movntps".to_string(), movntps::SAMPLES);
    samples.insert("movntq".to_string(), movntq::SAMPLES);
    samples.insert("movq".to_string(), movq::SAMPLES);
    samples.insert("movq2dq".to_string(), movq2dq::SAMPLES);
    samples.insert("movsd".to_string(), movsd::SAMPLES);
    samples.insert("movshdup".to_string(), movshdup::SAMPLES);
    samples.insert("movsldup".to_string(), movsldup::SAMPLES);
    samples.insert("movss".to_string(), movss::SAMPLES);
    samples.insert("movsx".to_string(), movsx::SAMPLES);
    samples.insert("movupd".to_string(), movupd::SAMPLES);
    samples.insert("movups".to_string(), movups::SAMPLES);
    samples.insert("movs".to_string(), movs::SAMPLES);
    samples.insert("movzx".to_string(), movzx::SAMPLES);
    samples.insert("mul".to_string(), mul::SAMPLES);
    samples.insert("mulpd".to_string(), mulpd::SAMPLES);
    samples.insert("mulps".to_string(), mulps::SAMPLES);
    samples.insert("mulsd".to_string(), mulsd::SAMPLES);
    samples.insert("mulss".to_string(), mulss::SAMPLES);
    samples.insert("mulx".to_string(), mulx::SAMPLES);
    samples.insert("neg".to_string(), neg::SAMPLES);
    samples.insert("not".to_string(), not::SAMPLES);
    samples.insert("or".to_string(), or::SAMPLES);
    samples.insert("orps".to_string(), orps::SAMPLES);
    samples.insert("pabsb".to_string(), pabsb::SAMPLES);
    samples.insert("pabsd".to_string(), pabsd::SAMPLES);
    samples.insert("pabsw".to_string(), pabsw::SAMPLES);
    samples.insert("paddd".to_string(), paddd::SAMPLES);
    samples.insert("paddq".to_string(), paddq::SAMPLES);
    samples.insert("paddsb".to_string(), paddsb::SAMPLES);
    samples.insert("paddsw".to_string(), paddsw::SAMPLES);
    samples.insert("paddusb".to_string(), paddusb::SAMPLES);
    samples.insert("paddusw".to_string(), paddusw::SAMPLES);
    samples.insert("paddw".to_string(), paddw::SAMPLES);
    samples.insert("palignr".to_string(), palignr::SAMPLES);
    samples.insert("pand".to_string(), pand::SAMPLES);
    samples.insert("pavgb".to_string(), pavgb::SAMPLES);
    samples.insert("pavgw".to_string(), pavgw::SAMPLES);
    samples.insert("pcmpeqb".to_string(), pcmpeqb::SAMPLES);
    samples.insert("pcmpeqd".to_string(), pcmpeqd::SAMPLES);
    samples.insert("pcmpeqq".to_string(), pcmpeqq::SAMPLES);
    samples.insert("pcmpeqw".to_string(), pcmpeqw::SAMPLES);
    samples.insert("pcmpistri".to_string(), pcmpistri::SAMPLES);
    samples.insert("pcmpgtb".to_string(), pcmpgtb::SAMPLES);
    samples.insert("pcmpgtd".to_string(), pcmpgtd::SAMPLES);
    samples.insert("pcmpgtq".to_string(), pcmpgtq::SAMPLES);
    samples.insert("pcmpgtw".to_string(), pcmpgtw::SAMPLES);
    samples.insert("pdep".to_string(), pdep::SAMPLES);
    samples.insert("pext".to_string(), pext::SAMPLES);
    samples.insert("pextrb".to_string(), pextrb::SAMPLES);
    samples.insert("pextrd".to_string(), pextrd::SAMPLES);
    samples.insert("pextrq".to_string(), pextrq::SAMPLES);
    samples.insert("pextrw".to_string(), pextrw::SAMPLES);
    samples.insert("phaddd".to_string(), phaddd::SAMPLES);
    samples.insert("phaddsw".to_string(), phaddsw::SAMPLES);
    samples.insert("phaddw".to_string(), phaddw::SAMPLES);
    samples.insert("phsubd".to_string(), phsubd::SAMPLES);
    samples.insert("phsubw".to_string(), phsubw::SAMPLES);
    samples.insert("pinsrb".to_string(), pinsrb::SAMPLES);
    samples.insert("pinsrd".to_string(), pinsrd::SAMPLES);
    samples.insert("pinsrq".to_string(), pinsrq::SAMPLES);
    samples.insert("pinsrw".to_string(), pinsrw::SAMPLES);
    samples.insert("pmaddubsw".to_string(), pmaddubsw::SAMPLES);
    samples.insert("pmaddwd".to_string(), pmaddwd::SAMPLES);
    samples.insert("pmaxsb".to_string(), pmaxsb::SAMPLES);
    samples.insert("pmaxsd".to_string(), pmaxsd::SAMPLES);
    samples.insert("pmaxsw".to_string(), pmaxsw::SAMPLES);
    samples.insert("pmaxub".to_string(), pmaxub::SAMPLES);
    samples.insert("pmaxud".to_string(), pmaxud::SAMPLES);
    samples.insert("pmaxuw".to_string(), pmaxuw::SAMPLES);
    samples.insert("pminsb".to_string(), pminsb::SAMPLES);
    samples.insert("pminsd".to_string(), pminsd::SAMPLES);
    samples.insert("pminsw".to_string(), pminsw::SAMPLES);
    samples.insert("pminub".to_string(), pminub::SAMPLES);
    samples.insert("pminud".to_string(), pminud::SAMPLES);
    samples.insert("pminuw".to_string(), pminuw::SAMPLES);
    samples.insert("pmovsxbd".to_string(), pmovsxbd::SAMPLES);
    samples.insert("pmovsxbq".to_string(), pmovsxbq::SAMPLES);
    samples.insert("pmovsxbw".to_string(), pmovsxbw::SAMPLES);
    samples.insert("pmovsxdq".to_string(), pmovsxdq::SAMPLES);
    samples.insert("pmovsxwd".to_string(), pmovsxwd::SAMPLES);
    samples.insert("pmovsxwq".to_string(), pmovsxwq::SAMPLES);
    samples.insert("pmovzxbd".to_string(), pmovzxbd::SAMPLES);
    samples.insert("pmovzxbq".to_string(), pmovzxbq::SAMPLES);
    samples.insert("pmovzxbw".to_string(), pmovzxbw::SAMPLES);
    samples.insert("pmovmskb".to_string(), pmovmskb::SAMPLES);
    samples.insert("pmovzxwd".to_string(), pmovzxwd::SAMPLES);
    samples.insert("pmovzxwq".to_string(), pmovzxwq::SAMPLES);
    samples.insert("pmovzxdq".to_string(), pmovzxdq::SAMPLES);
    samples.insert("pmulhw".to_string(), pmulhw::SAMPLES);
    samples.insert("pmulhrsw".to_string(), pmulhrsw::SAMPLES);
    samples.insert("pmulhuw".to_string(), pmulhuw::SAMPLES);
    samples.insert("pmulld".to_string(), pmulld::SAMPLES);
    samples.insert("pmullw".to_string(), pmullw::SAMPLES);
    samples.insert("pmuludq".to_string(), pmuludq::SAMPLES);
    samples.insert("pop".to_string(), pop::SAMPLES);
    samples.insert("popf".to_string(), popf::SAMPLES);
    samples.insert("popcnt".to_string(), popcnt::SAMPLES);
    samples.insert("por".to_string(), por::SAMPLES);
    samples.insert("push".to_string(), push::SAMPLES);
    samples.insert("pushf".to_string(), pushf::SAMPLES);
    samples.insert("packssdw".to_string(), packssdw::SAMPLES);
    samples.insert("packsswb".to_string(), packsswb::SAMPLES);
    samples.insert("packuswb".to_string(), packuswb::SAMPLES);
    samples.insert("paddb".to_string(), paddb::SAMPLES);
    samples.insert("pandn".to_string(), pandn::SAMPLES);
    samples.insert("psadbw".to_string(), psadbw::SAMPLES);
    samples.insert("pshufb".to_string(), pshufb::SAMPLES);
    samples.insert("pshufd".to_string(), pshufd::SAMPLES);
    samples.insert("pshufhw".to_string(), pshufhw::SAMPLES);
    samples.insert("pshuflw".to_string(), pshuflw::SAMPLES);
    samples.insert("pshufw".to_string(), pshufw::SAMPLES);
    samples.insert("psignb".to_string(), psignb::SAMPLES);
    samples.insert("psignd".to_string(), psignd::SAMPLES);
    samples.insert("psignw".to_string(), psignw::SAMPLES);
    samples.insert("pslld".to_string(), pslld::SAMPLES);
    samples.insert("pslldq".to_string(), pslldq::SAMPLES);
    samples.insert("psllq".to_string(), psllq::SAMPLES);
    samples.insert("psllw".to_string(), psllw::SAMPLES);
    samples.insert("psraw".to_string(), psraw::SAMPLES);
    samples.insert("psrad".to_string(), psrad::SAMPLES);
    samples.insert("psrld".to_string(), psrld::SAMPLES);
    samples.insert("psrldq".to_string(), psrldq::SAMPLES);
    samples.insert("psrlq".to_string(), psrlq::SAMPLES);
    samples.insert("psrlw".to_string(), psrlw::SAMPLES);
    samples.insert("psubb".to_string(), psubb::SAMPLES);
    samples.insert("psubd".to_string(), psubd::SAMPLES);
    samples.insert("psubq".to_string(), psubq::SAMPLES);
    samples.insert("psubusb".to_string(), psubusb::SAMPLES);
    samples.insert("psubusw".to_string(), psubusw::SAMPLES);
    samples.insert("psubw".to_string(), psubw::SAMPLES);
    samples.insert("ptest".to_string(), ptest::SAMPLES);
    samples.insert("punpckhbw".to_string(), punpckhbw::SAMPLES);
    samples.insert("punpckhdq".to_string(), punpckhdq::SAMPLES);
    samples.insert("punpckhqdq".to_string(), punpckhqdq::SAMPLES);
    samples.insert("punpckhwd".to_string(), punpckhwd::SAMPLES);
    samples.insert("punpcklbw".to_string(), punpcklbw::SAMPLES);
    samples.insert("punpckldq".to_string(), punpckldq::SAMPLES);
    samples.insert("punpcklqdq".to_string(), punpcklqdq::SAMPLES);
    samples.insert("punpcklwd".to_string(), punpcklwd::SAMPLES);
    samples.insert("pxor".to_string(), pxor::SAMPLES);
    samples.insert("rdtsc".to_string(), rdtsc::SAMPLES);
    samples.insert("ret".to_string(), ret::SAMPLES);
    samples.insert("rcl".to_string(), rcl::SAMPLES);
    samples.insert("rcr".to_string(), rcr::SAMPLES);
    samples.insert("rol".to_string(), rol::SAMPLES);
    samples.insert("ror".to_string(), ror::SAMPLES);
    samples.insert("rorx".to_string(), rorx::SAMPLES);
    samples.insert("sar".to_string(), sar::SAMPLES);
    samples.insert("sarx".to_string(), sarx::SAMPLES);
    samples.insert("scas".to_string(), scas::SAMPLES);
    samples.insert("seta".to_string(), seta::SAMPLES);
    samples.insert("setae".to_string(), setae::SAMPLES);
    samples.insert("setb".to_string(), setb::SAMPLES);
    samples.insert("setbe".to_string(), setbe::SAMPLES);
    samples.insert("setc".to_string(), setc::SAMPLES);
    samples.insert("sete".to_string(), sete::SAMPLES);
    samples.insert("setg".to_string(), setg::SAMPLES);
    samples.insert("setge".to_string(), setge::SAMPLES);
    samples.insert("setl".to_string(), setl::SAMPLES);
    samples.insert("setle".to_string(), setle::SAMPLES);
    samples.insert("setne".to_string(), setne::SAMPLES);
    samples.insert("setno".to_string(), setno::SAMPLES);
    samples.insert("setnp".to_string(), setnp::SAMPLES);
    samples.insert("setns".to_string(), setns::SAMPLES);
    samples.insert("setnz".to_string(), setnz::SAMPLES);
    samples.insert("seto".to_string(), seto::SAMPLES);
    samples.insert("setp".to_string(), setp::SAMPLES);
    samples.insert("sets".to_string(), sets::SAMPLES);
    samples.insert("setz".to_string(), setz::SAMPLES);
    samples.insert("shl".to_string(), shl::SAMPLES);
    samples.insert("shld".to_string(), shld::SAMPLES);
    samples.insert("shlx".to_string(), shlx::SAMPLES);
    samples.insert("shr".to_string(), shr::SAMPLES);
    samples.insert("shrd".to_string(), shrd::SAMPLES);
    samples.insert("shufpd".to_string(), shufpd::SAMPLES);
    samples.insert("shufps".to_string(), shufps::SAMPLES);
    samples.insert("shrx".to_string(), shrx::SAMPLES);
    samples.insert("sqrtpd".to_string(), sqrtpd::SAMPLES);
    samples.insert("sqrtsd".to_string(), sqrtsd::SAMPLES);
    samples.insert("sqrtss".to_string(), sqrtss::SAMPLES);
    samples.insert("stos".to_string(), stos::SAMPLES);
    samples.insert("sub".to_string(), sub::SAMPLES);
    samples.insert("subpd".to_string(), subpd::SAMPLES);
    samples.insert("subps".to_string(), subps::SAMPLES);
    samples.insert("subsd".to_string(), subsd::SAMPLES);
    samples.insert("subss".to_string(), subss::SAMPLES);
    samples.insert("stmxcsr".to_string(), stmxcsr::SAMPLES);
    samples.insert("system".to_string(), system::SAMPLES);
    samples.insert("tzcnt".to_string(), tzcnt::SAMPLES);
    samples.insert("ucomiss".to_string(), ucomiss::SAMPLES);
    samples.insert("ucomisd".to_string(), ucomisd::SAMPLES);
    samples.insert("unpckhpd".to_string(), unpckhpd::SAMPLES);
    samples.insert("unpckhps".to_string(), unpckhps::SAMPLES);
    samples.insert("unpcklpd".to_string(), unpcklpd::SAMPLES);
    samples.insert("unpcklps".to_string(), unpcklps::SAMPLES);
    samples.insert("vaddsubpd".to_string(), vaddsubpd::SAMPLES);
    samples.insert("vaddsubps".to_string(), vaddsubps::SAMPLES);
    samples.insert("vcmpss".to_string(), vcmpss::SAMPLES);
    samples.insert("vcomisd".to_string(), vcomisd::SAMPLES);
    samples.insert("vcomiss".to_string(), vcomiss::SAMPLES);
    samples.insert("vextracti128".to_string(), vextracti128::SAMPLES);
    samples.insert("vfmaddsd".to_string(), vfmaddsd::SAMPLES);
    samples.insert("vfmsubsd".to_string(), vfmsubsd::SAMPLES);
    samples.insert("vhaddpd".to_string(), vhaddpd::SAMPLES);
    samples.insert("vhaddps".to_string(), vhaddps::SAMPLES);
    samples.insert("vinsertf128".to_string(), vinsertf128::SAMPLES);
    samples.insert("vmovq".to_string(), vmovq::SAMPLES);
    samples.insert("vpaddq".to_string(), vpaddq::SAMPLES);
    samples.insert("vpaddb".to_string(), vpaddb::SAMPLES);
    samples.insert("vpaddd".to_string(), vpaddd::SAMPLES);
    samples.insert("vpaddsb".to_string(), vpaddsb::SAMPLES);
    samples.insert("vpaddsw".to_string(), vpaddsw::SAMPLES);
    samples.insert("vpaddusb".to_string(), vpaddusb::SAMPLES);
    samples.insert("vpaddusw".to_string(), vpaddusw::SAMPLES);
    samples.insert("vpaddw".to_string(), vpaddw::SAMPLES);
    samples.insert("vpinsrw".to_string(), vpinsrw::SAMPLES);
    samples.insert("vldmxcsr".to_string(), vldmxcsr::SAMPLES);
    samples.insert("vmaskmov".to_string(), vmaskmov::SAMPLES);
    samples.insert("vpcmpeqb".to_string(), vpcmpeqb::SAMPLES);
    samples.insert("vpcmpeqd".to_string(), vpcmpeqd::SAMPLES);
    samples.insert("vpcmpeqq".to_string(), vpcmpeqq::SAMPLES);
    samples.insert("vpcmpeqw".to_string(), vpcmpeqw::SAMPLES);
    samples.insert("vpcmpgtb".to_string(), vpcmpgtb::SAMPLES);
    samples.insert("vpcmpgtd".to_string(), vpcmpgtd::SAMPLES);
    samples.insert("vpcmpgtq".to_string(), vpcmpgtq::SAMPLES);
    samples.insert("vpcmpgtw".to_string(), vpcmpgtw::SAMPLES);
    samples.insert("vpbroadcastb".to_string(), vpbroadcastb::SAMPLES);
    samples.insert("vperm2i128".to_string(), vperm2i128::SAMPLES);
    samples.insert("vpermq".to_string(), vpermq::SAMPLES);
    samples.insert("vphaddd".to_string(), vphaddd::SAMPLES);
    samples.insert("vphaddsw".to_string(), vphaddsw::SAMPLES);
    samples.insert("vphaddw".to_string(), vphaddw::SAMPLES);
    samples.insert("vpmovmskb".to_string(), vpmovmskb::SAMPLES);
    samples.insert("vpmovsxbd".to_string(), vpmovsxbd::SAMPLES);
    samples.insert("vpmovsxbq".to_string(), vpmovsxbq::SAMPLES);
    samples.insert("vpmovsxbw".to_string(), vpmovsxbw::SAMPLES);
    samples.insert("vpmovsxdq".to_string(), vpmovsxdq::SAMPLES);
    samples.insert("vpmovsxwd".to_string(), vpmovsxwd::SAMPLES);
    samples.insert("vpmovsxwq".to_string(), vpmovsxwq::SAMPLES);
    samples.insert("vpmovzxbd".to_string(), vpmovzxbd::SAMPLES);
    samples.insert("vpmovzxbq".to_string(), vpmovzxbq::SAMPLES);
    samples.insert("vpmovzxbw".to_string(), vpmovzxbw::SAMPLES);
    samples.insert("vpmovzxdq".to_string(), vpmovzxdq::SAMPLES);
    samples.insert("vpmovzxwd".to_string(), vpmovzxwd::SAMPLES);
    samples.insert("vpmovzxwq".to_string(), vpmovzxwq::SAMPLES);
    samples.insert("vpsllq".to_string(), vpsllq::SAMPLES);
    samples.insert("vpsignw".to_string(), vpsignw::SAMPLES);
    samples.insert("vpsrlq".to_string(), vpsrlq::SAMPLES);
    samples.insert("vpsubd".to_string(), vpsubd::SAMPLES);
    samples.insert("vpsubq".to_string(), vpsubq::SAMPLES);
    samples.insert("vstmxcsr".to_string(), vstmxcsr::SAMPLES);
    samples.insert("vucomisd".to_string(), vucomisd::SAMPLES);
    samples.insert("vucomiss".to_string(), vucomiss::SAMPLES);
    samples.insert("vzeroupper".to_string(), vzeroupper::SAMPLES);
    samples.insert("verr".to_string(), verr::SAMPLES);
    samples.insert("xadd".to_string(), xadd::SAMPLES);
    samples.insert("xgetbv".to_string(), xgetbv::SAMPLES);
    samples.insert("xlat".to_string(), xlat::SAMPLES);
    samples
}

mod aaa;
mod aad;
mod aam;
mod aas;
mod adc;
mod adcx;
mod adox;
mod add;
mod addsubpd;
mod addsubps;
mod addpd;
mod addps;
mod addsd;
mod addss;
mod and;
mod andn;
mod andnpd;
mod andnps;
mod andpd;
mod andps;
mod bextr;
mod blsi;
mod blsmsk;
mod blsr;
mod bsf;
mod bsr;
mod bswap;
mod bt;
mod call;
mod cbw;
mod cdq;
mod cdqe;
mod cmovb;
mod btc;
mod btr;
mod bts;
mod bzhi;
mod cmovbe;
mod cmovc;
mod cmove;
mod cmovge;
mod cmovl;
mod cmovle;
mod cmovnb;
mod cmovnbe;
mod cmovne;
mod cmovnl;
mod cmovnle;
mod cmovno;
mod cmovnp;
mod cmovns;
mod cmovnz;
mod cmovo;
mod cmovp;
mod cmovs;
mod cmovz;
mod cmp;
mod cmps;
mod cmpss;
mod cmpxchg;
mod cmpxchg16b;
mod cmpxchg8b;
mod comiss;
mod comisd;
mod cpuid;
mod crc32;
mod cqo;
mod cwd;
mod cwde;
mod cvtdq2pd;
mod cvtdq2ps;
mod cvtpd2dq;
mod cvtpd2ps;
mod cvtpi2pd;
mod cvtpi2ps;
mod cvtps2dq;
mod cvtps2pd;
mod cvtsd2si;
mod cvtsd2ss;
mod cvtsi2sd;
mod cvtsi2ss;
mod cvtss2sd;
mod cvtss2si;
mod cvttpd2dq;
mod cvttps2dq;
mod cvttsd2si;
mod cvttss2si;
mod daa;
mod dec;
mod div;
mod divpd;
mod divps;
mod divsd;
mod divss;
mod enter;
mod extractps;
mod fabs;
mod f2xm1;
mod fadd;
mod faddp;
mod fbld;
mod fbstp;
mod fchs;
mod fcmov;
mod fcmovb;
mod fcmovbe;
mod fcmove;
mod fcmovnb;
mod fcmovnbe;
mod fcmovne;
mod fcmovnu;
mod fcmovu;
mod fcom;
mod fcomi;
mod fcomip;
mod fcomp;
mod fcompp;
mod fcos;
mod fdecstp;
mod fdiv;
mod fdivp;
mod fdivr;
mod fdivrp;
mod ffree;
mod ffreep;
mod fiadd;
mod fidiv;
mod fidivr;
mod fist;
mod fistp;
mod fisttp;
mod fimul;
mod fisub;
mod fisubr;
mod fild;
mod fincstp;
mod fnclex;
mod fnop;
mod fld;
mod fld1;
mod fldl2e;
mod fldl2t;
mod fldlg2;
mod fldln2;
mod fldpi;
mod fldz;
mod fmul;
mod fmulp;
mod fninit;
mod fnstsw;
mod fpatan;
mod fprem;
mod fprem1;
mod fptan;
mod frndint;
mod fscale;
mod fsin;
mod fsincos;
mod fsqrt;
mod fst;
mod fstp;
mod ftst;
mod fsub;
mod fsubp;
mod fsubr;
mod fsubrp;
mod fucom;
mod fucomi;
mod fucomip;
mod fucomp;
mod fucompp;
mod fxam;
mod fxch;
mod fyl2x;
mod fyl2xp1;
mod haddpd;
mod haddps;
mod idiv;
mod imul;
mod inc;
mod int3;
mod jae;
mod je;
mod jmp;
mod jne;
mod kmovw;
mod lar;
mod lahf;
mod lddqu;
mod lea;
mod leave;
mod ldmxcsr;
mod lods;
mod lzcnt;
mod minsd;
mod minps;
mod minss;
mod maskmovq;
mod mov;
mod movabs;
mod movapd;
mod movaps;
mod movbe;
mod movd;
mod movddup;
mod movdq2q;
mod movdqa;
mod movdqu;
mod movhlps;
mod movhpd;
mod movhps;
mod movlhps;
mod movlpd;
mod movlps;
mod movmskpd;
mod movmskps;
mod movntdq;
mod movnti;
mod movntpd;
mod movntps;
mod movntq;
mod movq;
mod movq2dq;
mod movs;
mod movsd;
mod movshdup;
mod movsldup;
mod movss;
mod movsx;
mod movupd;
mod movups;
mod movzx;
mod mul;
mod mulpd;
mod mulps;
mod mulsd;
mod mulss;
mod mulx;
mod neg;
mod nop;
mod not;
mod or;
mod orps;
mod pabsb;
mod pabsd;
mod pabsw;
mod packssdw;
mod packsswb;
mod packuswb;
mod paddb;
mod paddd;
mod paddq;
mod paddsb;
mod paddsw;
mod paddusb;
mod paddusw;
mod paddw;
mod palignr;
mod pand;
mod pandn;
mod pavgb;
mod pavgw;
mod pcmpeqb;
mod pcmpeqd;
mod pcmpeqq;
mod pcmpeqw;
mod pcmpistri;
mod pcmpgtb;
mod pcmpgtd;
mod pcmpgtq;
mod pcmpgtw;
mod pdep;
mod pext;
mod pextrb;
mod pextrd;
mod pextrq;
mod pextrw;
mod phaddd;
mod phaddsw;
mod phaddw;
mod phsubd;
mod phsubw;
mod pinsrb;
mod pinsrd;
mod pinsrq;
mod pinsrw;
mod pmaddubsw;
mod pmaddwd;
mod pmaxsb;
mod pmaxsd;
mod pmaxsw;
mod pmaxub;
mod pmaxud;
mod pmaxuw;
mod pminsb;
mod pminsd;
mod pminsw;
mod pminub;
mod pminud;
mod pminuw;
mod pmovmskb;
mod pmovsxbd;
mod pmovsxbq;
mod pmovsxbw;
mod pmovsxdq;
mod pmovsxwd;
mod pmovsxwq;
mod pmovzxbd;
mod pmovzxbq;
mod pmovzxbw;
mod pmovzxdq;
mod pmovzxwd;
mod pmovzxwq;
mod pmulhw;
mod pmulhrsw;
mod pmulhuw;
mod pmulld;
mod pmullw;
mod pmuludq;
mod pop;
mod popf;
mod popcnt;
mod por;
mod psadbw;
mod pshufb;
mod pshufd;
mod pshufhw;
mod pshuflw;
mod pshufw;
mod psignb;
mod psignd;
mod psignw;
mod pslld;
mod pslldq;
mod psllq;
mod psllw;
mod psrad;
mod psraw;
mod psrld;
mod psrldq;
mod psrlq;
mod psrlw;
mod psubb;
mod psubd;
mod psubq;
mod psubusb;
mod psubusw;
mod psubw;
mod ptest;
mod punpckhbw;
mod punpckhdq;
mod punpckhqdq;
mod punpckhwd;
mod punpcklbw;
mod punpckldq;
mod punpcklqdq;
mod punpcklwd;
mod push;
mod pushf;
mod pxor;
mod rdtsc;
mod rcl;
mod rcr;
mod ret;
mod rol;
mod ror;
mod rorx;
mod sar;
mod sarx;
mod sbb;
mod scas;
mod seta;
mod setae;
mod setb;
mod setbe;
mod setc;
mod sete;
mod setg;
mod setge;
mod setl;
mod setle;
mod setne;
mod setno;
mod setnp;
mod setns;
mod setnz;
mod seto;
mod setp;
mod sets;
mod setz;
mod shl;
mod shld;
mod shlx;
mod shr;
mod shrd;
mod shufpd;
mod shufps;
mod shrx;
mod sqrtpd;
mod sqrtsd;
mod sqrtss;
mod stos;
mod sub;
mod subpd;
mod subps;
mod subsd;
mod subss;
mod system;
mod test;
mod tzcnt;
mod ucomiss;
mod ucomisd;
mod unpckhpd;
mod unpckhps;
mod unpcklpd;
mod unpcklps;
mod vextracti128;
mod vaddsubpd;
mod vaddsubps;
mod vcmpss;
mod vcomisd;
mod vcomiss;
mod vfmaddsd;
mod vfmsubsd;
mod vhaddpd;
mod vhaddps;
mod vinsertf128;
mod vmovq;
mod vpaddq;
mod vpaddb;
mod vpaddd;
mod vpaddsb;
mod vpaddsw;
mod vpaddusb;
mod vpaddusw;
mod vpaddw;
mod vpinsrw;
mod vldmxcsr;
mod vmaskmov;
mod vpcmpeqb;
mod vpcmpeqd;
mod vpcmpeqq;
mod vpcmpeqw;
mod vpcmpgtb;
mod vpcmpgtd;
mod vpcmpgtq;
mod vpcmpgtw;
mod vpbroadcastb;
mod vperm2i128;
mod vpermq;
mod vphaddd;
mod vphaddsw;
mod vphaddw;
mod vpmovmskb;
mod vpmovsxbd;
mod vpmovsxbq;
mod vpmovsxbw;
mod vpmovsxdq;
mod vpmovsxwd;
mod vpmovsxwq;
mod vpmovzxbd;
mod vpmovzxbq;
mod vpmovzxbw;
mod vpmovzxdq;
mod vpmovzxwd;
mod vpmovzxwq;
mod vpsllq;
mod vpsignw;
mod vpsrlq;
mod vpsubd;
mod vpsubq;
mod vstmxcsr;
mod stmxcsr;
mod vucomisd;
mod vucomiss;
mod vzeroupper;
mod verr;
mod xgetbv;
mod xlat;
mod xadd;
mod xchg;
mod xor;
