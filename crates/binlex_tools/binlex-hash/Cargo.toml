[package]
name = "binlex-hash"
version = "2.0.0"
edition = "2024"

[[bin]]
name = "binlex-hash"
path = "src/main.rs"

[dependencies]
binlex = { path = "../../.." }
clap = { version = "4.5.27", features = ["derive"] }
